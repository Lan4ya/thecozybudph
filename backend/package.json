{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pkg:generate": "supabase gen types typescript --local > ../packages/schemas/src/types/supabase.types.ts && mkdir -p  ./supabase/functions/_shared/schemas && rm -rf ./supabase/functions/_shared/schemas/* && cp -r ../packages/schemas/src/* ./supabase/functions/_shared/schemas/",
    "db:apply-schemas": "pnpm exec drizzle-kit generate && supabase migrations up --include-all"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.1.0",
    "@snaplet/copycat": "^6.0.0",
    "@snaplet/seed": "^0.98.0",
    "@types/pg": "^8.16.0",
    "dotenv": "^17.2.3",
    "drizzle-kit": "^0.31.9",
    "drizzle-seed": "^0.3.1",
    "tsx": "^4.21.0"
  }
}
