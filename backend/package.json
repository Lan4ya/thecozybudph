{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc .",
    "gen-supabase-types": "pnpx supabase gen types typescript --local > ../packages/schema/src/db/supabase.types.ts && mkdir -p  ./supabase/functions/_shared/schema && rm -rf ./supabase/functions/_shared/schema/* && cp -r ../packages/schema/src/* ./supabase/functions/_shared/schema/"
  },
  "devDependencies": {
    "@TheCozyBud/schema": "workspace:*",
    "dotenv": "^17.2.3",
    "supabase": "^2.67.0",
    "tsx": "^4.21.0"
  }
}
